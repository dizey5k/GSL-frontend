// Auth & Claims
export type ClaimSource = 'capt' | 'gangame' | 'both'

// Player / Family
export type PlayerMode = 'players' | 'families' | 'gangame'
export type CompareMode = 'players' | 'families' | 'gangame'

// Wall
export type WallPostStatus = 'pending' | 'approved' | 'rejected'

// Duel (Gangame)
export type DuelResult = 'win' | 'loss' | 'draw'

export type ClubRole = 'SCOUT' | 'CALLER' | 'SHOOTER'
